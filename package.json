{
  "name": "nitelog",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r --parallel build",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "docker-up": "pnpm docker compose up -d",
    "docker-down": "pnpm docker compose down"
  },
  "dependencies": {
    "pnpm": "^10.30.3"
  }
}
